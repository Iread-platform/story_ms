image : mcr.microsoft.com/dotnet/sdk:5.0

stages:
  - build
  - publish

build:
  stage: build
  script:
    - dotnet build

publish:
  stage: publish
#  only:
#    - develop
  artifacts:
    paths:
      - publish/
  script:
    - dotnet publish -o ./publish