image : mcr.microsoft.com/dotnet/sdk:5.0

stages:
  - build
  - release

build:
  stage: build
  script:
    - dotnet build

release:
  stage: publish
#  only:
#    - develop
  artifacts:
    paths:
      - publish/
  script:
    - dotnet publish -o ./publish